// Main application entry point
// This file imports the cart manager and initializes other functionality

document.addEventListener('DOMContentLoaded', () => {
    initializeApp();
});

function initializeApp() {
    console.log('Nexo Digital App initialized');
}
